# 项目记忆

记录项目开发过程中的已知问题和特殊规则。**只记录代码里看不出来的内容**。

---

## 已知问题

### 前端 SSR
- 影响 CSS 布局的关键样式必须放全局 `main.css`，避免 scoped CSS 导致 SSR 样式闪烁
- `useMessage()`、`useDialog()` 等不能在 SSR 阶段调用，应放在事件处理函数内或使用 `.client.vue` 后缀

### 后端
（暂无记录）

---

## 特殊业务规则

（暂无记录）

---

## 历史踩坑

（记录实际遇到的问题和解决方案，避免重复踩坑）
