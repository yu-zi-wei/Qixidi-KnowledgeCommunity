<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.1</version>
        <relativePath/>
    </parent>

    <groupId>com.light</groupId>
    <artifactId>qixidi-service</artifactId>
    <packaging>pom</packaging>
    <version>${revision}</version>

    <properties>
        <revision>1.0.1-SNAPSHOT</revision>
        <java.version>17</java.version>
        <java.source.version>${java.version}</java.source.version>
        <java.target.version>${java.version}</java.target.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!--        springBoot -->
        <spring-boot.version>4.0.1</spring-boot.version>
        <!-- Boot 4 parent 不再管理 aspectjweaver，需自行维护 -->
        <aspectjweaver.version>1.9.25</aspectjweaver.version>

        <!--        通用工具包 -->
        <hutool.version>5.8.27</hutool.version>
        <guava.version>30.0-jre</guava.version>

        <!--       easyexcel工具 -->
        <easyexcel.version>3.3.4</easyexcel.version>

        <!--敏感词相关-->
        <commons-collections4.version>4.4</commons-collections4.version>
        <assertj-core.version>3.25.3</assertj-core.version>

        <!--        解析ip地址归属地-->
        <ip2region.version>2.7.0</ip2region.version>

        <!--        第三方登录-->
        <JustAuth.version>1.16.5</JustAuth.version>

        <aliyun.dysmsapi20170525.version>3.1.1</aliyun.dysmsapi20170525.version>

        <!-- DB & MyBatis libs（lombok/aspectjweaver/mysql-connector-j/jsqlparser 均由 spring-boot-parent 或 mybatis-plus-jsqlparser 统一管理） -->
        <p6spy.version>3.9.1</p6spy.version>
        <mybatis-plus.version>3.5.17</mybatis-plus.version>
        <druid.version>1.2.28</druid.version>
        <dynamic-datasource.version>4.5.0</dynamic-datasource.version>
        <redisson.version>4.7.0</redisson.version>

        <!-- OSS 配置 -->
        <aliyun.oss.version>3.14.0</aliyun.oss.version>
        <alipay-sdk-java.version>4.15.6.ALL</alipay-sdk-java.version>

        <!-- minio 配置 -->
        <minio.version>8.3.8</minio.version>
        <!--        图片压缩工具-->
        <thumbnailator.version>0.4.20</thumbnailator.version>
        <webp-imageio.version>0.1.6</webp-imageio.version>

        <!-- Sa-Token libs -->
        <sa-token.version>1.46.0</sa-token.version>

    </properties>

    <modules>
        <module>qixidi-startup</module>
        <module>framework</module>
        <module>qixidi-system</module>
        <module>qixidi-auth</module>
        <module>qixidi-business</module>
        <module>qixidi-common</module>
        <module>qixidi-ai</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.light</groupId>
                <artifactId>qixidi-system</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.light</groupId>
                <artifactId>qixidi-startup</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>qixidi-auth</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>qixidi-ai</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>framework</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.light</groupId>
                <artifactId>light-mybatis-plus</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>light-core</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>light-exception</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>light-excel</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>light-redission</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>light-oss</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.light</groupId>
                <artifactId>light-web</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>qixidi-business</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>qixidi-common</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>light-minio</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>light-ai</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.light</groupId>
                <artifactId>light-web-socket</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- ========================================== 其他依赖=================================-->

            <!-- alipay 支付宝支付-->
            <dependency>
                <groupId>com.alipay.sdk</groupId>
                <artifactId>alipay-sdk-java</artifactId>
                <version>${alipay-sdk-java.version}</version>
                <exclusions>
                    <exclusion>
                        <artifactId>fastjson</artifactId>
                        <groupId>com.alibaba</groupId>
                    </exclusion>
                </exclusions>
            </dependency>

            <!-- spring-boot-starter-aop：Boot 4 已移除该 starter，AOP 由 spring-context + aspectjweaver 覆盖 -->

            <!-- Sa-Token 权限认证，在线文档：https://sa-token.cc -->
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-spring-boot4-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>

            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-redis-template</artifactId>
                <version>${sa-token.version}</version>
            </dependency>

            <dependency>
                <groupId>com.aliyun.oss</groupId>
                <artifactId>aliyun-sdk-oss</artifactId>
                <version>${aliyun.oss.version}</version>
            </dependency>

            <dependency>
                <groupId>net.coobird</groupId>
                <artifactId>thumbnailator</artifactId>
                <version>${thumbnailator.version}</version>
            </dependency>

            <dependency>
                <groupId>org.sejda.imageio</groupId>
                <artifactId>webp-imageio</artifactId>
                <version>${webp-imageio.version}</version>
            </dependency>

            <dependency>
                <groupId>io.minio</groupId>
                <artifactId>minio</artifactId>
                <version>${minio.version}</version>
            </dependency>


            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>easyexcel</artifactId>
                <version>${easyexcel.version}</version>
                <exclusions>
                    <exclusion>
                        <groupId>org.apache.poi</groupId>
                        <artifactId>poi</artifactId>
                    </exclusion>
                    <exclusion>
                        <groupId>org.apache.poi</groupId>
                        <artifactId>poi-ooxml-schemas</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>

            <!-- 阿里数据库连接池 -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-4-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- dynamic-datasource 多数据源-->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>dynamic-datasource-spring-boot4-starter</artifactId>
                <version>${dynamic-datasource.version}</version>
            </dependency>


            <!-- 统一 guava 版本 解决隐式漏洞问题 -->
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>

            <!--敏感词相关-->
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj-core.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-collections4</artifactId>
                <version>${commons-collections4.version}</version>
            </dependency>

            <!--redisson-->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>
            <!-- redisson 4.x 将 CacheConfig/RedissonSpringCacheManager 拆到独立构件，且在 starter 中为 optional，需显式引入 -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-cache</artifactId>
                <version>${redisson.version}</version>
            </dependency>

            <!--        JustAuth第三方登录开放平台-->
            <dependency>
                <groupId>me.zhyd.oauth</groupId>
                <artifactId>JustAuth</artifactId>
                <version>${JustAuth.version}</version>
            </dependency>

            <!--            阿里短信发送-->
            <!-- https://mvnrepository.com/artifact/com.aliyun/dysmsapi20170525 -->
            <dependency>
                <groupId>com.aliyun</groupId>
                <artifactId>dysmsapi20170525</artifactId>
                <version>${aliyun.dysmsapi20170525.version}</version>
            </dependency>

            <!-- hutool 的依赖配置-->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-bom</artifactId>
                <version>${hutool.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-web</artifactId>
                <version>${spring-boot.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-test</artifactId>
                <scope>test</scope>
                <version>${spring-boot.version}</version>
            </dependency>

            <dependency>
                <groupId>org.aspectj</groupId>
                <artifactId>aspectjweaver</artifactId>
                <version>${aspectjweaver.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- 3.5.9+ 分页等插件依赖的 SQL 解析器独立成模块 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <dependency>
                <groupId>p6spy</groupId>
                <artifactId>p6spy</artifactId>
                <version>${p6spy.version}</version>
            </dependency>

            <!-- mysql-connector-j / lombok 版本由 spring-boot-parent 统一管理 -->

            <!-- https://mvnrepository.com/artifact/org.lionsoul/ip2region -->
            <dependency>
                <groupId>org.lionsoul</groupId>
                <artifactId>ip2region</artifactId>
                <version>${ip2region.version}</version>
            </dependency>


            <!--             Source: https://mvnrepository.com/artifact/org.springframework.ai/spring-ai-bom -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>2.0.1</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
