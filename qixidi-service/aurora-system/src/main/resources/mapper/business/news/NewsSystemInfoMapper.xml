<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aurora.business.mapper.news.NewsSystemInfoMapper">
    <resultMap type="com.aurora.business.domain.entity.news.NewsSystemInfo" id="NewsSystemInfoResult">
        <result property="id" column="id"/>
        <result property="newsTitle" column="news_title"/>
        <result property="newsContent" column="news_content"/>
        <result property="isDetails" column="is_details"/>
        <result property="type" column="type"/>
        <result property="createTime" column="create_time"/>
        <result property="lapseTime" column="lapse_time"/>
    </resultMap>

    <select id="selectBaseUid" resultType="com.aurora.business.domain.vo.news.NewsSystemInfoVo">
        SELECT id
        FROM news_system_info
        WHERE state = 0
          AND type = 2
          AND is_mass_air = 1
        UNION
        SELECT id
        FROM news_system_info
        WHERE state = 0
          AND type = 2
          AND is_mass_air = 2
          AND uid = #{uid}
    </select>

    <select id="selectUid" resultType="com.aurora.business.domain.vo.news.NewsUserInfoVo">
        SELECT id,
               news_title   as newsTitle,
               news_content as newsContent,
               is_details,
               uid,
               create_time
        FROM news_system_info
        WHERE state = 0
          AND type = 2
          AND is_mass_air = 2
          AND uid = #{uid}
        UNION
        SELECT id,
               news_title   as newsTitle,
               news_content as newsContent,
               is_details,
               uid,
               create_time
        FROM news_system_info
        WHERE state = 0
          AND type = 2
          AND is_mass_air = 1
        order by create_time desc
    </select>
</mapper>
