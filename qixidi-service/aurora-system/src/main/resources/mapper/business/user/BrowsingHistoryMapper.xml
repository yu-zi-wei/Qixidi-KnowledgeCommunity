<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aurora.business.mapper.user.BrowsingHistoryMapper">
    <resultMap type="com.aurora.business.domain.entity.user.BrowsingHistory" id="BrowsingHistoryResult">
        <result property="id" column="id"/>
        <result property="uid" column="uid"/>
        <result property="targetId" column="target_id"/>
        <result property="targetTitle" column="target_title"/>
        <result property="targetUid" column="target_uid"/>
        <result property="targetType" column="target_type"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectIsTime" resultType="com.aurora.business.domain.vo.user.BrowsingHistoryVo">
        SELECT id,
               uid,
               target_id
        FROM b_browsing_history
        WHERE uid = #{uuid}
          and target_type = #{targetType}
          and target_id = #{targetId}
          and LEFT ( update_time
            , 13 )= #{hour}
    </select>

    <insert id="insertOrUpdates">
        insert into b_browsing_history (uid, target_id, target_title, target_uid, target_type, create_time, update_time)
        values (#{add.uid}, #{add.targetId}, #{add.targetTitle}, #{add.targetUid}, #{add.targetType}, #{add.createTime},
                #{add.updateTime}) ON DUPLICATE KEY
        UPDATE update_time =
        VALUES (update_time);
    </insert>
</mapper>
